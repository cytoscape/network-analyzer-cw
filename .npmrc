# react-chart-editor pins plotly.js <3 in its peerDependencies, but it works with
# plotly.js 3.x when the plotly instance is passed as a prop (see PlotDialog.tsx);
# without this flag plain `npm install` fails on that stale peer range.
legacy-peer-deps=true
