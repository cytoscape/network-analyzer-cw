{
  "//": [
    "App sources only. Build tooling (vite.config.ts) lives in",
    "tsconfig.node.json; the Node-test compile lives in tsconfig.test.json.",
    "",
    "The `types` list is what pulls in the ambient `cyweb/*` modules,",
    "`virtual:cyweb-app-meta` and Vite's `*?worker&inline` declarations — do",
    "NOT set `typeRoots`, it suppresses the default lookup that finds these.",
    "(src/types/untyped-modules.d.ts is picked up via `include`.)",
    "",
    "skipLibCheck: api-types 1.0.0-beta.3 dist ships a broken bare `ReactNode`",
    "reference; our own sources are still fully checked."
  ],
  "include": ["src/**/*"],
  "exclude": ["src/**/*.test.ts"],
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",

    "noImplicitAny": true,
    "strictNullChecks": true,
    "isolatedModules": true,
    "noEmit": true,

    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,

    "skipLibCheck": true,

    "types": [
      "@cytoscape-web/api-types",
      "@cytoscape-web/app-runtime/meta",
      "vite/client"
    ]
  }
}
